int main(void) {
    int x;
    int y;
    int z[5];
    int aCharacter;

    /* Should work */
    aCharacter = 'x';
    aCharacter = 'x' + 'y';
    y = z[1];
    z[3] = 3;
    return -(1 + 4 * 3 / 5);
    return 'x';

    /* Shouldn't work */
    x = 5 + 1 - "fjdafkdajlfkad";
    z[3] = "this should not work";
    z = 7;
    y = 1 + (3 < 4);
    y = 3 < 4 < 5;
    y = z["type error"];
    return !(1 + 3);
    return "testing";
}
